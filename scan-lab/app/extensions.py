"""Extension registry for future Flask integrations."""


def init_extensions(app):
    """Initialize Flask extensions in one place."""
    # Future extension hooks live here, for example:
    # db.init_app(app)
    # login_manager.init_app(app)
    _ = app
